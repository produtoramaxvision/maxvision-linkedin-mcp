{
  "name": "@maxvision/linkedin-license",
  "version": "0.1.0",
  "private": true,
  "description": "License server (Cloudflare Worker) for MaxVision LinkedIn Suite",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.96.0"
  }
}
