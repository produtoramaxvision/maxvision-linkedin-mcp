{
  "name": "@maxvision/linkedin-landing",
  "version": "0.1.0",
  "private": true,
  "description": "MaxVision LinkedIn Suite — landing page (Cloudflare Pages, static + Pages Functions)",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2026-05-08 --compatibility-flags=nodejs_compat",
    "deploy": "wrangler pages deploy . --project-name=linkedin-maxvision-landing --commit-dirty=true"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "wrangler": "^3.96.0"
  }
}
